import { TierMultiplierToCreate, TierToCreate } from '../../services/tier/tier.service';

const multiplierToCreate: TierMultiplierToCreate = { creatingUserId: 1, tierId: 2, multiplier: 3 };

export default {
	multiplierToCreate
};
